"use client";

import { useEffect, useMemo, useState } from "react";
import AppLayout from "@/components/layout/AppLayout";
import { supabase } from "@/lib/supabase";

type Purchase={id:string;supplier:string|null;invoice_reference:string|null;purchase_date:string|null;payment_method:string|null;total_amount:number|null;notes:string|null;created_at:string};
const money=(n:number)=>`₦${Number(n||0).toLocaleString("en-NG",{maximumFractionDigits:0})}`;
export default function PurchasesPage(){const[data,setData]=useState<Purchase[]>([]),[q,setQ]=useState(""),[loading,setLoading]=useState(true),[error,setError]=useState("");useEffect(()=>{load()},[]);async function load(){setLoading(true);const{data,error}=await supabase.from("inventory_purchases").select("id,supplier,invoice_reference,purchase_date,payment_method,total_amount,notes,created_at").order("created_at",{ascending:false}).limit(200);if(error)setError(error.message);setData((data??[])as Purchase[]);setLoading(false)}const filtered=useMemo(()=>data.filter(p=>`${p.supplier??""} ${p.invoice_reference??""} ${p.payment_method??""}`.toLowerCase().includes(q.toLowerCase())),[data,q]);const total=filtered.reduce((s,p)=>s+Number(p.total_amount||0),0);return <AppLayout><div className="space-y-6"><header className="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.16em] text-teal-600">Procurement</p><h1 className="mt-1 text-3xl font-bold tracking-tight text-slate-950">Purchase history</h1><p className="mt-1 text-sm text-slate-500">Track supplier purchases and the cost of stock received.</p></div><button onClick={load} className="h-10 rounded-xl border border-slate-200 bg-white px-4 text-sm font-semibold text-slate-700">Refresh</button></header><section className="grid gap-4 sm:grid-cols-3"><Card title="Purchases" value={String(filtered.length)}/><Card title="Purchase value" value={money(total)}/><Card title="Suppliers" value={String(new Set(filtered.map(p=>p.supplier).filter(Boolean)).size)}/></section><section className="rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="border-b border-slate-200 p-4"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search supplier, invoice or payment method..." className="h-10 w-full rounded-xl border border-slate-200 px-3 text-sm outline-none focus:border-teal-500"/></div>{loading?<p className="p-8 text-center text-sm text-slate-400">Loading purchases…</p>:error?<p className="p-8 text-center text-sm text-red-600">{error}</p>:<div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-400"><tr><th className="px-5 py-3">Supplier</th><th className="px-5 py-3">Reference</th><th className="px-5 py-3">Date</th><th className="px-5 py-3">Method</th><th className="px-5 py-3 text-right">Amount</th></tr></thead><tbody className="divide-y divide-slate-100">{filtered.map(p=><tr key={p.id} className="hover:bg-slate-50"><td className="px-5 py-4 font-semibold text-slate-900">{p.supplier||"—"}</td><td className="px-5 py-4 text-slate-500">{p.invoice_reference||"—"}</td><td className="px-5 py-4 text-slate-500">{p.purchase_date?new Date(p.purchase_date).toLocaleDateString("en-NG"):new Date(p.created_at).toLocaleDateString("en-NG")}</td><td className="px-5 py-4 capitalize text-slate-500">{p.payment_method||"—"}</td><td className="px-5 py-4 text-right font-semibold text-slate-900">{money(Number(p.total_amount||0))}</td></tr>)}{!filtered.length&&<tr><td colSpan={5} className="p-10 text-center text-sm text-slate-400">No purchases recorded yet.</td></tr>}</tbody></table></div>}</section></div></AppLayout>}
function Card({title,value}:{title:string;value:string}){return <div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p className="text-xs font-semibold uppercase tracking-wide text-slate-400">{title}</p><p className="mt-2 text-2xl font-bold text-slate-950">{value}</p></div>}
