"use client";

import { useEffect, useMemo, useState } from "react";
import AppLayout from "@/components/layout/AppLayout";
import { businessOperationsService } from "@/services/businessOperationsService";

type Balance={customer_id:string;customer_name:string;phone:string;debit:number;credit:number;balance:number};
const money=(n:number)=>`₦${Number(n||0).toLocaleString("en-NG",{minimumFractionDigits:2})}`;
export default function OutstandingPage(){const [rows,setRows]=useState<Balance[]>([]);const [loading,setLoading]=useState(true);const [q,setQ]=useState("");useEffect(()=>{businessOperationsService.getCustomerBalances().then(r=>{setRows((r.data??[]) as Balance[]);setLoading(false)})},[]);const filtered=useMemo(()=>rows.filter(x=>`${x.customer_name} ${x.phone}`.toLowerCase().includes(q.toLowerCase())),[rows,q]);const total=rows.reduce((a,x)=>a+Number(x.balance),0);return <AppLayout><div className="space-y-7"><header><p className="text-xs font-semibold uppercase tracking-[0.16em] text-teal-600">Receivables</p><h1 className="mt-1 text-3xl font-bold tracking-tight text-slate-950">Outstanding Balances</h1><p className="mt-1 text-sm text-slate-500">One source of truth for customer debt across repairs, sales and invoices.</p></header><div className="grid gap-4 sm:grid-cols-3"><Stat label="Customers owing" value={String(rows.length)}/><Stat label="Total outstanding" value={money(total)}/><Stat label="Largest balance" value={money(Math.max(0,...rows.map(x=>Number(x.balance))))}/></div><section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="flex flex-col gap-3 border-b border-slate-100 p-5 sm:flex-row sm:items-center sm:justify-between"><h2 className="font-semibold">Customer ledger</h2><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search customer or phone" className="h-10 w-full rounded-xl border border-slate-200 px-3 text-sm outline-none focus:border-teal-500 sm:w-72"/></div>{loading?<div className="p-8 text-sm text-slate-500">Loading balances…</div>:filtered.length===0?<div className="p-8 text-sm text-slate-500">No outstanding customer balances.</div>:<div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-5 py-3">Customer</th><th className="px-5 py-3">Phone</th><th className="px-5 py-3 text-right">Charged</th><th className="px-5 py-3 text-right">Paid</th><th className="px-5 py-3 text-right">Balance</th></tr></thead><tbody>{filtered.map(x=><tr key={x.customer_id} className="border-t border-slate-100"><td className="px-5 py-4 font-semibold">{x.customer_name}</td><td className="px-5 py-4 text-slate-500">{x.phone}</td><td className="px-5 py-4 text-right">{money(x.debit)}</td><td className="px-5 py-4 text-right">{money(x.credit)}</td><td className="px-5 py-4 text-right font-bold text-amber-700">{money(x.balance)}</td></tr>)}</tbody></table></div>}</section></div></AppLayout>}
function Stat({label,value}:{label:string;value:string}){return <div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p className="text-xs font-semibold uppercase tracking-wide text-slate-400">{label}</p><p className="mt-2 text-2xl font-bold text-slate-950">{value}</p></div>}
