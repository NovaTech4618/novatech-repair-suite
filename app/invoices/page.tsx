"use client";

import { useEffect, useMemo, useState } from "react";
import AppLayout from "@/components/layout/AppLayout";
import { businessOperationsService } from "@/services/businessOperationsService";

type Invoice = { id:string; invoice_number:string; status:string; subtotal:number; discount:number; total:number; issued_at:string; due_at:string|null; customers?: {full_name:string;phone:string}|{full_name:string;phone:string}[]|null };
const money=(n:number)=>`₦${Number(n||0).toLocaleString("en-NG",{minimumFractionDigits:2})}`;

export default function InvoicesPage(){
 const [items,setItems]=useState<Invoice[]>([]); const [loading,setLoading]=useState(true); const [error,setError]=useState("");
 const [number,setNumber]=useState(""); const [customer,setCustomer]=useState(""); const [total,setTotal]=useState(""); const [saving,setSaving]=useState(false);
 const load=async()=>{setLoading(true);const r=await businessOperationsService.getInvoices();setError(r.error?.message??"");setItems((r.data??[]) as Invoice[]);setLoading(false)};
 useEffect(()=>{load()},[]);
 const summary=useMemo(()=>({total:items.length,paid:items.filter(x=>x.status==="paid").length,outstanding:items.filter(x=>x.status!=="paid"&&x.status!=="void").reduce((a,x)=>a+Number(x.total||0),0)}),[items]);
 async function create(){const amount=Number(total);if(!number.trim()||!amount){setError("Invoice number and total are required.");return}setSaving(true);const r=await businessOperationsService.createInvoice({invoiceNumber:number.trim(),customerId:customer.trim()||null,subtotal:amount,total:amount});if(r.error)setError(r.error.message);else{setNumber("");setCustomer("");setTotal("");await load()}setSaving(false)}
 return <AppLayout><div className="space-y-7">
  <header><p className="text-xs font-semibold uppercase tracking-[0.16em] text-teal-600">Receivables</p><h1 className="mt-1 text-3xl font-bold tracking-tight text-slate-950">Invoices & Receipts</h1><p className="mt-1 text-sm text-slate-500">Issue professional customer charges and keep every receivable traceable.</p></header>
  <div className="grid gap-4 sm:grid-cols-3"><Stat label="Invoices" value={String(summary.total)}/><Stat label="Paid" value={String(summary.paid)}/><Stat label="Outstanding value" value={money(summary.outstanding)}/></div>
  <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="font-semibold text-slate-950">Create invoice</h2><div className="mt-4 grid gap-3 md:grid-cols-4"><input value={number} onChange={e=>setNumber(e.target.value)} placeholder="Invoice number" className="h-10 rounded-xl border border-slate-200 px-3 text-sm outline-none focus:border-teal-500"/><input value={customer} onChange={e=>setCustomer(e.target.value)} placeholder="Customer ID (optional)" className="h-10 rounded-xl border border-slate-200 px-3 text-sm outline-none focus:border-teal-500"/><input value={total} onChange={e=>setTotal(e.target.value)} type="number" min="0" placeholder="Total (₦)" className="h-10 rounded-xl border border-slate-200 px-3 text-sm outline-none focus:border-teal-500"/><button disabled={saving} onClick={create} className="h-10 rounded-xl bg-teal-600 px-4 text-sm font-semibold text-white hover:bg-teal-700 disabled:opacity-50">{saving?"Creating…":"Create invoice"}</button></div>{error&&<p className="mt-3 text-sm text-red-600">{error}</p>}</section>
  <section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="border-b border-slate-100 px-5 py-4 font-semibold">Invoice register</div>{loading?<div className="p-8 text-sm text-slate-500">Loading invoices…</div>:items.length===0?<div className="p-8 text-sm text-slate-500">No invoices yet.</div>:<div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-5 py-3">Invoice</th><th className="px-5 py-3">Customer</th><th className="px-5 py-3">Date</th><th className="px-5 py-3">Status</th><th className="px-5 py-3 text-right">Total</th></tr></thead><tbody>{items.map(i=><tr key={i.id} className="border-t border-slate-100"><td className="px-5 py-4 font-semibold text-slate-900">{i.invoice_number}</td><td className="px-5 py-4 text-slate-600">{Array.isArray(i.customers)?i.customers[0]?.full_name:(i.customers?.full_name??"Walk-in customer")}</td><td className="px-5 py-4 text-slate-500">{new Date(i.issued_at).toLocaleDateString()}</td><td className="px-5 py-4"><span className="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold capitalize text-slate-700">{i.status.replace("_"," ")}</span></td><td className="px-5 py-4 text-right font-semibold">{money(i.total)}</td></tr>)}</tbody></table></div>}</section>
 </div></AppLayout>
}
function Stat({label,value}:{label:string;value:string}){return <div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p className="text-xs font-semibold uppercase tracking-wide text-slate-400">{label}</p><p className="mt-2 text-2xl font-bold text-slate-950">{value}</p></div>}
