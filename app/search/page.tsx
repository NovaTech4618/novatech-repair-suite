"use client";
import { useEffect,useState } from "react";
import Link from "next/link";
import { Search as SearchIcon,Users,Smartphone,Wrench,Package,FileText,Truck } from "lucide-react";
import AppLayout from "@/components/layout/AppLayout";
import { supabase } from "@/lib/supabase";
type Result={entity_type:string;id:string;title:string;subtitle:string;href:string};
const meta:Record<string,{label:string;Icon:typeof Users}>={customer:{label:"Customer",Icon:Users},device:{label:"Device",Icon:Smartphone},repair:{label:"Repair",Icon:Wrench},inventory:{label:"Inventory",Icon:Package},invoice:{label:"Invoice",Icon:FileText},purchase:{label:"Purchase",Icon:Truck}};
export default function SearchPage(){const[q,setQ]=useState("");const[results,setResults]=useState<Result[]>([]);const[loading,setLoading]=useState(false);const[error,setError]=useState("");useEffect(()=>{const t=setTimeout(async()=>{const term=q.trim();if(!term){setResults([]);return}setLoading(true);setError("");const{data,error:e}=await supabase.rpc("global_search",{p_query:term,p_limit:100});if(e)setError(e.message);setResults((data??[])as Result[]);setLoading(false)},250);return()=>clearTimeout(t)},[q]);const groups=Object.entries(results.reduce<Record<string,Result[]>>((a,r)=>(a[r.entity_type]??=[]).push(r),{}));return <AppLayout><div className="mx-auto max-w-5xl space-y-6"><div><p className="text-xs font-semibold uppercase tracking-[0.16em] text-teal-600">Workspace</p><h1 className="mt-1 text-3xl font-bold tracking-tight text-slate-950">Global Search</h1><p className="mt-1 text-sm text-slate-500">Find customers, devices, repairs, stock, invoices and supplier purchases from one place.</p></div><div className="relative"><SearchIcon className="absolute left-4 top-1/2 size-5 -translate-y-1/2 text-slate-400"/><input autoFocus value={q} onChange={e=>setQ(e.target.value)} placeholder="Search name, phone, IMEI, SKU, repair issue, invoice or supplier…" className="h-14 w-full rounded-2xl border border-slate-200 bg-white pl-12 pr-4 text-sm shadow-sm outline-none focus:border-teal-500 focus:ring-4 focus:ring-teal-500/10"/></div>{loading&&<p className="text-sm text-slate-400">Searching…</p>}{error&&<p className="text-sm text-red-600">{error}</p>}{q.trim()&&!loading&&!error&&<p className="text-sm text-slate-500">{results.length} result{results.length===1?"":"s"} for <span className="font-semibold text-slate-800">“{q}”</span></p>}{!q.trim()?<div className="rounded-2xl border border-dashed border-slate-200 bg-white p-12 text-center"><SearchIcon className="mx-auto size-8 text-slate-300"/><p className="mt-3 font-semibold text-slate-800">Start typing to search NOVATECH</p><p className="mt-1 text-sm text-slate-500">Search across your operational data without opening each module.</p></div>:!loading&&!results.length&&!error?<div className="rounded-2xl border border-slate-200 bg-white p-12 text-center"><p className="font-semibold text-slate-800">No results found</p><p className="mt-1 text-sm text-slate-500">Try a customer name, phone number, IMEI, SKU, invoice number or supplier.</p></div>:<div className="space-y-5">{groups.map(([type,items])=>{const m=meta[type]??{label:type,Icon:SearchIcon};const Icon=m.Icon;return <section key={type} className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="border-b border-slate-100 px-5 py-3 text-xs font-bold uppercase tracking-wide text-slate-400">{m.label} · {items.length}</div><div className="divide-y divide-slate-100">{items.map(r=><Link key={r.entity_type+r.id} href={r.href} className="flex items-center gap-4 p-4 transition hover:bg-slate-50"><div className="flex size-10 shrink-0 items-center justify-center rounded-xl bg-slate-100 text-slate-600"><Icon className="size-5"/></div><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold text-slate-900">{r.title}</p><p className="truncate text-xs text-slate-500">{r.subtitle}</p></div><span className="text-xs font-semibold text-teal-600">Open →</span></Link>)}</div></section>})}</div>}</div></AppLayout>}
