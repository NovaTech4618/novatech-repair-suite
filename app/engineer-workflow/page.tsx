"use client";

import { useEffect, useState } from "react";
import AppLayout from "@/components/layout/AppLayout";
import { businessOperationsService } from "@/services/businessOperationsService";
import { engineerService } from "@/services/engineerService";

type Engineer={id:string;name:string;status:string};type Perf=Engineer&{total_repairs:number;completed_repairs:number;active_repairs:number;repair_revenue:number};
const money=(n:number)=>`₦${Number(n||0).toLocaleString("en-NG",{minimumFractionDigits:2})}`;
export default function EngineerWorkflowPage(){const [engineers,setEngineers]=useState<Engineer[]>([]);const [perf,setPerf]=useState<Perf[]>([]);const [repairId,setRepairId]=useState("");const [engineerId,setEngineerId]=useState("");const [message,setMessage]=useState("");useEffect(()=>{engineerService.getEngineers().then(r=>setEngineers((r.data??[]) as Engineer[]));businessOperationsService.getEngineerPerformance().then(r=>setPerf((r.data??[]) as Perf[]))},[]);async function assign(){if(!repairId||!engineerId)return;const r=await businessOperationsService.assignRepair(repairId,engineerId);setMessage(r.error?r.error.message:"Repair assigned successfully.");if(!r.error)businessOperationsService.getEngineerPerformance().then(x=>setPerf((x.data??[]) as Perf[]))}return <AppLayout><div className="space-y-7"><header><p className="text-xs font-semibold uppercase tracking-[0.16em] text-teal-600">Workshop operations</p><h1 className="mt-1 text-3xl font-bold tracking-tight text-slate-950">Engineer Workflow</h1><p className="mt-1 text-sm text-slate-500">Assign ownership, monitor workload and measure repair performance.</p></header><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="font-semibold">Assign repair</h2><div className="mt-4 grid gap-3 md:grid-cols-[1fr_1fr_auto]"><input value={repairId} onChange={e=>setRepairId(e.target.value)} placeholder="Repair ID" className="h-10 rounded-xl border border-slate-200 px-3 text-sm outline-none focus:border-teal-500"/><select value={engineerId} onChange={e=>setEngineerId(e.target.value)} className="h-10 rounded-xl border border-slate-200 px-3 text-sm outline-none focus:border-teal-500"><option value="">Select engineer</option>{engineers.map(e=><option key={e.id} value={e.id}>{e.name} · {e.status}</option>)}</select><button onClick={assign} className="h-10 rounded-xl bg-teal-600 px-5 text-sm font-semibold text-white hover:bg-teal-700">Assign</button></div>{message&&<p className="mt-3 text-sm text-slate-600">{message}</p>}</section><section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="border-b border-slate-100 px-5 py-4 font-semibold">Engineer performance</div><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-5 py-3">Engineer</th><th className="px-5 py-3">Active</th><th className="px-5 py-3">Completed</th><th className="px-5 py-3">Total</th><th className="px-5 py-3 text-right">Revenue</th></tr></thead><tbody>{perf.map(e=><tr key={e.engineer_id} className="border-t border-slate-100"><td className="px-5 py-4 font-semibold">{e.name}</td><td className="px-5 py-4">{e.active_repairs}</td><td className="px-5 py-4">{e.completed_repairs}</td><td className="px-5 py-4">{e.total_repairs}</td><td className="px-5 py-4 text-right font-semibold">{money(e.repair_revenue)}</td></tr>)}</tbody></table></div></section></div></AppLayout>}
