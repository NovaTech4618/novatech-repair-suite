-- Audit logs are authoritative records. The client must not be able to forge
-- arbitrary audit entries through the SECURITY DEFINER writer. Operational
-- audit entries are generated by database triggers instead.
revoke all on function public.write_audit_log(text,text,uuid,jsonb,jsonb,jsonb) from public, anon, authenticated;
